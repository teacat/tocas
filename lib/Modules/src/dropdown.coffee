class TocasDropdown
    $name:
        'dropdown'
    $data:
        {}
    $options:
        onChange  : ->
        onAdd     : ->
        onRemove  : ->
        onNoResult: ->
        onSelect  : ->
        onShow    : ->
        onHide    : ->

    $init: ->

    $methods: ->
        toggle: ->
        show: ->
        hide: ->
        'set text': ->
        'set value': ->
        'get text': ->
        'get value': ->
        'set selected': ->
        'remove selected': ->
        'change values': ->
        'hide others': ->


