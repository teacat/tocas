// Generated by CoffeeScript 2.0.0-beta4
var TocasTab;

TocasTab = (function() {
  class TocasTab {
    $init({$this}) {
      // 綁定分頁按鈕的點擊事件。
      return $this.on('click', function() {
        var tabGroup, tabName;
        // 如果點擊的是正在啟用的分頁則略過。
        if ($this.hasClass('active')) {
          return;
        }
        tabName = $this.attr('data-tab');
        // 忽略不是分頁用的項目按鈕。
        if (tabName === null) {
          return;
        }
        // 取得分頁群組名稱。
        return tabGroup = $this.attr('data-tab-group');
      });
    }

  };

  TocasTab.prototype.$name = 'tab';

  TocasTab.prototype.$options = {
    onSwitch: function() {} // 當分頁籤切換時所會呼叫的函式。
  };

  // 呼叫切換回呼函式。
  TocasTab.prototype.$methods = {
    // 切換到指定的分頁。
    'change tab': function({$elements}) {},
    // 回傳一個指定分頁是否被啟用的布林值。
    'is active': function({$elements}, name) {}
  };

  return TocasTab;

})();
